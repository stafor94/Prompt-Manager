# 버전 관리 정책

이 프로젝트는 앱 배포 버전에 `MAJOR.MINOR.PATCH` 형식을 사용합니다.

예시: `v1.3.2`

화면에 표시할 때는 `v` 접두사를 붙이고, 코드와 배포 메타데이터에는 필요에 따라 숫자 부분만 사용할 수 있습니다.

## 1. 버전 증가 기준

### MAJOR

기존 버전과의 호환성을 유지하기 어렵거나 사용자 데이터·사용 흐름에 근본적인 변화가 발생할 때 증가합니다.

적용 사례:

- 기존 데이터 구조와 호환되지 않는 변경
- 이전 백업 파일을 복원할 수 없는 백업 스키마 변경
- 주요 화면 구조 또는 핵심 사용 흐름의 전면 개편
- 기존 핵심 기능 제거 또는 동작 의미의 근본적 변경
- 별도 마이그레이션 없이는 기존 데이터를 사용할 수 없는 변경

증가 예시:

```text
v1.8.4 → v2.0.0
```

MAJOR가 증가하면 MINOR와 PATCH는 `0`으로 초기화합니다.

### MINOR

기존 데이터와 기능의 하위 호환성을 유지하면서 새로운 사용자 기능을 추가할 때 증가합니다.

적용 사례:

- 신규 화면 또는 탭 추가
- 이미지 첨부, 보관함, 백업·복원 등 주요 기능 추가
- 새로운 검색·필터·정렬 조건 추가
- 프롬프트 복제 등 새로운 사용자 동작 추가
- 새로운 설정 항목 추가
- 기존 기능을 유지하는 의미 있는 UI·UX 확장

증가 예시:

```text
v1.2.5 → v1.3.0
```

MINOR가 증가하면 PATCH는 `0`으로 초기화합니다.

### PATCH

기존 기능 범위 안에서 오류를 수정하거나 작은 개선을 반영할 때 증가합니다.

적용 사례:

- 버튼, 이미지, 목록 또는 입력 동작 오류 수정
- PWA 캐시 및 업데이트 반영 문제 수정
- 레이아웃 잘림, 간격, 크기, 정렬 조정
- 문구, 라벨 또는 아이콘 변경
- 성능, 안정성 또는 접근성 개선
- 사용자 동작을 변경하지 않는 내부 리팩터링
- 호환성을 유지하는 의존성 또는 빌드 설정 업데이트

증가 예시:

```text
v1.3.0 → v1.3.1
```

## 2. 변경이 여러 개 포함된 경우

한 배포에 여러 종류의 변경이 포함되면 가장 높은 단계를 적용합니다.

```text
MAJOR + MINOR + PATCH → MAJOR 증가
MINOR + PATCH         → MINOR 증가
PATCH만 포함          → PATCH 증가
```

예를 들어 현재 버전이 `v1.4.3`일 때:

- 보관함 필터 버튼 위치만 조정: `v1.4.4`
- 보관함에 새로운 6열 보기 기능 추가: `v1.5.0`
- 이전 백업과 호환되지 않는 포맷 도입: `v2.0.0`

## 3. 버전을 올리지 않는 변경

다음 변경은 사용자에게 배포되는 동작이나 산출물에 영향을 주지 않는 한 버전을 올리지 않습니다.

- 문서만 수정
- 주석 또는 코드 포맷 정리
- 개발 중 임시 커밋
- 동일 배포 범위 안에서 발견된 추가 수정
- 빌드 결과에 반영되지 않는 내부 작업

버전은 커밋마다 증가시키지 않고, 사용자에게 배포 가능한 변경 단위마다 한 번 증가시킵니다.

## 4. 배포 시 동기화 대상

버전을 변경할 때 다음 위치의 값이 서로 일치해야 합니다.

- 앱 화면에 표시되는 버전
- 애플리케이션 코드의 버전 상수
- PWA 정적 파일의 쿼리 버전
- Service Worker 캐시 이름 또는 캐시 무효화 값
- 매니페스트 등 배포 메타데이터에 버전 값이 있는 경우 해당 값
- `CHANGELOG.md`의 릴리즈 노트
- 릴리즈 태그와 GitHub Release를 사용하는 경우 해당 버전과 본문

코드 수정이 반영됐지만 기존 Service Worker 캐시가 남을 수 있는 경우, 캐시 이름도 함께 변경하여 설치된 PWA가 새 자산을 받도록 합니다.

## 5. 릴리즈 노트 정책

모든 버전 증가는 [`CHANGELOG.md`](CHANGELOG.md)의 해당 버전 릴리즈 노트와 함께 처리합니다. 릴리즈 노트가 없는 버전 증가는 완료된 배포로 간주하지 않습니다.

### 작성 시점

- 개발 중 사용자 영향 변경은 `미출시` 항목에 누적합니다.
- 배포 시 `미출시` 내용을 실제 버전과 배포 날짜로 확정합니다.
- 확정 후 비어 있는 새 `미출시` 항목을 문서 최상단에 다시 생성합니다.
- 버전 변경과 릴리즈 노트는 가능한 한 동일한 커밋 또는 동일한 배포 PR에 포함합니다.

### 작성 형식

버전 항목은 다음 형식을 사용합니다.

```markdown
## [1.4.0] - 2026-08-06

### 추가

- 사용자가 알 수 있는 신규 기능

### 변경

- 기존 동작 또는 UI의 변경

### 수정

- 해결된 오류
```

변경 내용에 맞는 섹션만 사용합니다.

- `추가`: 신규 기능
- `변경`: 기존 동작이나 UI 변경
- `수정`: 오류 및 안정성 문제 해결
- `제거`: 삭제된 기능
- `호환성 주의`: 기존 데이터, 백업 또는 사용법과 호환되지 않는 변경
- `데이터 마이그레이션`: 사용자가 수행해야 하는 데이터 이전 절차
- `알려진 제한사항`: 배포 시점에 남아 있는 주요 제약

### 작성 원칙

- 단순 커밋 메시지 목록이 아니라 사용자 관점에서 달라진 내용을 작성합니다.
- 내부 파일명이나 구현 세부사항만 나열하지 않습니다.
- 데이터 손실 가능성, 백업 호환성, 수동 조치가 필요한 변경은 반드시 명시합니다.
- MAJOR 버전은 `호환성 주의`와 필요한 마이그레이션 절차를 반드시 검토합니다.
- 변경 사항이 없는 버전 항목은 만들지 않습니다.
- 과거 내역을 복원할 때 근거가 없는 변경 내용이나 버전은 추정해서 작성하지 않습니다.

GitHub Release를 생성하는 경우 본문은 `CHANGELOG.md`의 해당 버전 항목을 기준으로 작성합니다.

## 6. 버전 결정 및 배포 절차

1. 배포에 포함할 변경 목록을 확정합니다.
2. 각 변경을 MAJOR, MINOR, PATCH 또는 버전 증가 없음으로 분류합니다.
3. 가장 높은 변경 단계를 선택합니다.
4. 상위 단계 증가 시 하위 숫자를 `0`으로 초기화합니다.
5. `CHANGELOG.md`에 해당 버전과 배포 날짜의 릴리즈 노트를 작성합니다.
6. 코드, 화면 표시, 캐시 및 배포 메타데이터의 버전을 동일하게 변경합니다.
7. 릴리즈 노트와 실제 변경 내용이 일치하는지 검토합니다.
8. 빌드와 핵심 기능을 확인한 뒤 커밋 및 배포합니다.
9. 태그나 GitHub Release를 사용하는 경우 동일 버전으로 생성합니다.

## 7. 프롬프트별 버전과의 구분

앱 배포 버전과 저장된 프롬프트의 버전은 서로 독립적으로 관리합니다.

- 앱 배포 버전: `vMAJOR.MINOR.PATCH`
- 프롬프트 버전: 동일한 제목의 프롬프트 계열에서 `v1`, `v2`, `v3` 순으로 증가

기존 프롬프트를 수정하는 경우 해당 프롬프트의 버전은 유지합니다. 동일 제목으로 새 항목을 저장하거나 복제하는 경우 기존 최대 버전에 `1`을 더합니다.
