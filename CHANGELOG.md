# 변경 이력

이 문서는 Prompt Manager의 사용자 영향이 있는 변경 사항을 버전별로 기록합니다.

날짜는 한국 표준시(KST)를 기준으로 하며, 최신 버전부터 역순으로 작성합니다. 과거 내역은 저장소의 버전 변경 커밋과 기능 커밋을 기준으로 복원했습니다. 저장소에서 명시적인 버전 근거를 확인할 수 없는 번호는 임의로 작성하지 않았습니다.

## [미출시]

현재 예정된 변경 사항이 없습니다.


## [1.7.0] - 2026-09-09

### 변경

- 프롬프트 하나에 첨부할 수 있는 이미지 수를 최대 5장에서 10장으로 늘렸습니다.
- 편집기의 이미지 개수 표시와 초과 선택 안내를 10장 기준으로 갱신했습니다.
- ZIP·JSON 백업 및 복원 검증에서도 프롬프트당 최대 10장의 이미지를 허용합니다.

### 호환성

- 프롬프트 및 백업 데이터 구조는 변경하지 않아 기존 데이터와 기존 백업을 그대로 사용할 수 있습니다.
- 백업 `schemaVersion`은 유지하며 중복 판정 기준도 변경하지 않습니다.

### 테스트

- 10장 첨부·백업 왕복과 11장 초과 거부를 검증하는 회귀 테스트를 추가했습니다.
- 앱 버전과 Service Worker 캐시를 `v1.7.0` / `v52` 기준으로 갱신했습니다.

## [1.6.0] - 2026-09-09

### 추가

- 설정에서 이름이 10자 이하인 사용자 정의 LLM을 추가할 수 있습니다.
- 추가한 LLM은 프롬프트 작성·수정, 목록 필터, 이미지 보관함 필터, 최근 선택 LLM 기억에서 기본 LLM과 동일하게 동작합니다.
- 사용자 정의 LLM 목록을 IndexedDB에 저장하고 ZIP·JSON 백업 및 복원에 포함합니다.

### 호환성

- IndexedDB 버전을 3으로 올려 `customLlms` 저장소를 추가하며 기존 프롬프트와 첨부 데이터는 변경하지 않습니다.
- ZIP 백업 스키마는 4로 확장하지만 기존 스키마 3 ZIP과 스키마 1·2 JSON 백업을 계속 복원할 수 있습니다.
- 중복 프롬프트 판정 기준은 기존과 같이 `LLM 종류 + 제목 + 본문`을 유지합니다.

### 테스트

- 사용자 정의 LLM 이름 길이·중복·ID 정규화, 백업 왕복, UI/캐시 연결을 검증하는 회귀 테스트를 추가했습니다.
- 앱 버전과 Service Worker 캐시를 `v1.6.0` / `v51` 기준으로 갱신했습니다.

## [1.5.6] - 2026-08-17

### 수정

- 보관함 점진 로딩 시 선택된 2·3·4·6열 기준으로 화면에 표시되는 마지막 행이 채워지도록 이미지 묶음을 정렬해 렌더링합니다.
- 한 프롬프트에서 필요한 수보다 많은 이미지를 읽은 경우 남는 이미지는 메모리에 잠시 보류하고 다음 추가 로드에 이어 사용해 불필요한 IndexedDB 재읽기를 피합니다.
- 열 수를 변경했을 때 현재 마지막 행이 비어 있으면 남은 이미지가 있는 경우 필요한 수만 추가로 표시해 행을 채웁니다. 실제 데이터가 끝난 마지막 행은 이미지 누락 없이 그대로 표시합니다.
- 하단 추가 로드 스와이프 아이콘의 아래쪽 여백을 줄여 이미지 목록과 하단 영역 사이 간격을 더 촘촘하게 조정했습니다.

### 테스트

- 2·3·4·6열에서 25장 중 24장만 먼저 표시하고 1장을 다음 로드로 보류하는 행 정렬 동작을 검증하는 테스트를 추가했습니다.
- 열 수 변경 시 부족한 칸만 채우는 동작과 마지막 데이터에서는 남은 이미지를 모두 표시하는 동작을 검증합니다.
- 앱 버전과 Service Worker 캐시를 `v1.5.6` / `v50` 기준으로 갱신했습니다.

## [1.5.5] - 2026-08-17

### 수정

- 보관함 하단의 `N / 전체 장수 로드됨 · 목록 끝에서 위로 밀어 더 불러오기` 문구를 제거했습니다.
- 추가 이미지가 남아 있으면 하단에 위쪽 스와이프를 의미하는 이중 화살표 애니메이션만 표시하도록 변경했습니다.
- 로딩 중에는 안내 아이콘을 숨기고, 다음 묶음을 불러올 수 있을 때만 노출합니다.
- 보관함 상단 이미지 수는 현재 로드 수 대신 필터 조건에 해당하는 전체 이미지 수를 표시합니다.
- `prefers-reduced-motion` 환경에서는 안내 애니메이션을 비활성화합니다.

### 테스트

- 보관함 진행 문구가 제거되고 스와이프 안내 아이콘과 접근성 문구가 존재하는지 확인하는 회귀 테스트를 추가했습니다.
- 앱 버전과 Service Worker 캐시를 `v1.5.5` 기준으로 갱신했습니다.

## [1.5.4] - 2026-08-17

### 성능

- 보관함 진입 시 모든 첨부 이미지 원본을 한 번에 읽지 않고 경량 요약 목록만 먼저 확인한 뒤 약 24장 단위의 첫 묶음만 로드하도록 변경했습니다.
- 목록 끝에 도달한 상태에서 위로 스와이프하거나 마우스 휠을 더 내리면 다음 이미지 묶음을 추가로 읽도록 변경했습니다.
- 추가 로드는 최대 12개 프롬프트 레코드씩 하나의 IndexedDB 읽기 트랜잭션에서 처리하고, 이미지 디코딩은 비동기로 요청합니다.
- 보관함 LLM 필터를 변경하면 선택된 LLM의 요약만 대상으로 페이지를 다시 구성해 불필요한 이미지 원본 읽기를 줄였습니다.
- 보관함을 떠나면 기존과 같이 로드된 이미지 DOM과 메모리 참조를 해제합니다.

### 테스트

- 보관함 요약 필터링, 약 24장 단위 페이지 분할, 프롬프트 배치 제한을 검증하는 테스트를 추가했습니다.
- 보관함 코드가 전체 프롬프트 원본 조회 대신 요약 조회와 배치 레코드 조회를 사용하는지 확인하는 회귀 테스트를 추가했습니다.

## [1.5.3] - 2026-08-17

### 성능

- 프롬프트 목록·검색·태그 필터가 첨부 이미지 `data:` URL 전체를 반복해서 읽지 않도록 경량 `promptSummaries` IndexedDB 저장소를 추가했습니다.
- 기존 데이터는 최초 업데이트 시 레코드별로 요약 메타데이터를 생성하며, 원본 프롬프트와 첨부 이미지는 기존 `prompts` 저장소에 그대로 유지합니다.
- 이미지 보관함은 보관함 탭을 실제로 열 때만 전체 이미지 데이터를 읽고, 다른 탭으로 이동하면 이미지 DOM과 메모리 참조를 해제하도록 변경했습니다.
- 보관함 LLM 필터가 필터 변경이나 목록 갱신 때 IndexedDB 전체 프롬프트를 다시 읽지 않도록 렌더링된 이미지 메타데이터를 사용합니다.
- 프롬프트 저장·즐겨찾기·삭제·복원 시 원본과 요약 메타데이터를 같은 IndexedDB 트랜잭션에서 갱신해 두 저장소가 어긋나지 않도록 했습니다.

### 호환성

- IndexedDB 버전을 2로 올리지만 기존 `prompts` 레코드 형식과 ZIP·JSON 백업 형식은 변경하지 않았습니다.
- 첫 `v1.5.3` 실행에서는 기존 프롬프트 수에 따라 요약 메타데이터 생성 시간이 한 번 발생할 수 있습니다.

### 테스트

- 요약 데이터에서 이미지 본문이 제외되고 이미지 개수·제목·본문·태그·버전이 유지되는지 검증하는 테스트를 추가했습니다.
- 목록과 보관함 필터 경로가 전체 이미지 데이터 재읽기를 피하는지 정적 회귀 테스트를 추가했습니다.

## [1.5.2] - 2026-08-08

### 수정

- 이미지 뷰어의 해상도와 비율을 `1080×1920(9:16)`처럼 해상도 뒤에 괄호로 비율을 붙이는 형식으로 변경했습니다.
- 2장보기의 좌우 이미지 정보는 `왼쪽-1080×1920(9:16)`, `오른쪽-720×1080(2:3)`처럼 표시하도록 변경했습니다.
- 기존 파일명 미표시와 2장보기 제목 `외 1` 표기는 그대로 유지합니다.

### 테스트

- `720×1080 → 720×1080(2:3)`을 포함해 해상도·비율 표시 형식을 검증하는 테스트를 갱신했습니다.
- 2장보기의 `왼쪽-...`, `오른쪽-...` 메타 정보 형식을 확인하는 회귀 테스트를 갱신했습니다.

## [1.5.1] - 2026-08-08

### 수정

- 1장 이미지 뷰어 하단에서 파일명을 제거하고 프롬프트 제목, 이미지 비율, 원본 해상도만 표시하도록 정리했습니다.
- 상세 화면에서 연 1장 이미지에도 현재 프롬프트 제목이 표시되도록 보완했습니다.
- 2장 이미지 뷰어에서는 왼쪽 이미지의 프롬프트 제목 하나만 표시하고 뒤에 `외 1`을 붙이도록 변경했습니다.
- 2장 이미지 뷰어에서도 파일명은 표시하지 않습니다.
- 클릭 직후 기존 뷰어 코드가 파일명을 잠깐 표시하지 않도록 캡션 갱신을 마이크로태스크에서 우선 적용했습니다.

### 테스트

- 1장 캡션에 파일명이 포함되지 않고 제목·비율·해상도만 포함되는지 확인하는 회귀 테스트를 추가했습니다.
- 2장 캡션이 왼쪽 제목 하나와 `외 1` 형식으로 표시되는지 확인하는 테스트를 추가했습니다.

## [1.5.0] - 2026-08-07

### 추가

- 1장 이미지 뷰어 하단에 현재 이미지의 비율과 원본 해상도를 표시하도록 했습니다.
- 2장 이미지 뷰어 하단에는 왼쪽·오른쪽 이미지의 비율과 원본 해상도를 각각 표시하도록 했습니다.
- 이미지 비율은 원본 픽셀 크기의 최대공약수로 단순화해 `3:4`, `9:16`, `4:3` 같은 형태로 표시합니다.

### 변경

- 이미지가 로드되거나 스와이프로 다른 이미지로 이동할 때 하단 메타 정보가 현재 이미지 기준으로 자동 갱신됩니다.
- 2장보기에서는 기존 1장 뷰어의 캡션 갱신 로직이 두 장 메타 정보를 덮어쓰지 않도록 보기 모드를 분리했습니다.
- 신규 이미지 메타데이터 계산 모듈을 오프라인 앱 셸 캐시에 포함하고 앱 버전을 `v1.5.0`으로 갱신했습니다.

### 테스트

- `1080×1440 → 3:4`, `1080×1920 → 9:16`, `4032×3024 → 4:3` 비율 단순화와 해상도 표시를 검증하는 테스트를 추가했습니다.
- 신규 이미지 메타데이터 모듈과 `v1.5.0` 자산이 Service Worker 앱 셸에 포함되는지 확인하는 회귀 테스트를 추가했습니다.

## [1.4.2] - 2026-08-07

### 수정

- 보관함 `2장보기`에서 기존 1장 뷰어를 먼저 연 뒤 두 장 모드로 전환하던 비동기 연결 방식을 제거했습니다.
- `2장보기` 선택 상태에서는 이미지 클릭을 캡처 단계에서 직접 처리해 기존 1장 뷰어 열기 핸들러까지 이벤트가 전달되지 않도록 하고, 두 장 전용 뷰어를 즉시 열도록 변경했습니다.
- 두 장 뷰어의 포인터·키보드 탐색을 캡처 단계에서 처리해 기존 1장 단위 스와이프와 경합하지 않고 두 장 단위로 이동하도록 수정했습니다.
- 두 장 뷰어의 닫기와 뒤로가기 기록을 독립적으로 처리하도록 보강했습니다.
- 수정된 뷰어 코드가 설치형 PWA에도 즉시 반영되도록 정적 자산 버전과 Service Worker 앱 셸 캐시를 갱신했습니다.

### 테스트

- `2장보기` 클릭이 기존 1장 뷰어로 전달되지 않고 두 장 뷰어를 직접 여는지 확인하는 회귀 테스트를 추가했습니다.
- 두 장 보기 제스처가 캡처 단계에서 처리되고 기존 1장 단위 스와이프보다 우선하는지 확인하는 회귀 테스트를 추가했습니다.

## [1.4.1] - 2026-08-07

### 수정

- 보관함 `2장보기`를 선택해도 이미지 화면 맞춤 로직이 우측 이미지를 스와이프 임시 이미지로 오인해 제거하면서 한 장만 표시되던 문제를 수정했습니다.
- 이미지 화면 맞춤 시 `aria-hidden="true"`인 스와이프 전환용 임시 이미지만 제거하고 `2장보기`의 두 번째 이미지는 유지하도록 정리 범위를 제한했습니다.
- 수정된 뷰어 코드가 설치형 PWA에도 즉시 반영되도록 정적 자산 버전과 Service Worker 앱 셸 캐시를 갱신했습니다.

### 테스트

- 화면 맞춤 정리 로직이 전체 보조 이미지를 제거하지 않고 스와이프 미리보기만 대상으로 하는지 확인하는 회귀 테스트를 추가했습니다.

## [1.4.0] - 2026-08-07

### 추가

- 보관함 상단에 `1장보기`와 `2장보기` 확대 보기 선택을 추가했습니다.
- `2장보기`에서는 선택한 이미지와 다음 이미지를 화면 좌우에 한 장씩 표시하고, 마지막 이미지를 선택하면 직전 이미지와 함께 표시하도록 했습니다.
- `2장보기`에서도 좌우 스와이프와 방향키로 두 장 단위 탐색을 지원하며 `프롬프트별` 보기에서는 해당 프롬프트 이미지 범위만 사용합니다.
- 선택한 확대 보기 방식은 로컬에 저장되어 재실행 후에도 유지됩니다.

### 변경

- 보관함 보기 제어 행의 좌우 여백과 버튼 간격을 줄이고 확대 보기 선택을 가장 오른쪽에 배치했습니다.
- 작은 화면에서는 기존 제어가 눌리지 않도록 확대 보기 선택을 다음 행으로 배치합니다.
- 신규 보관함 확대 보기 모듈과 스타일을 오프라인 앱 셸 캐시에 포함하고 앱 버전을 `v1.4.0`으로 갱신했습니다.

### 테스트

- 단일·2장 보기 모드 정규화, 두 장 페어링, 마지막 이미지 처리, 두 장 단위 이동 및 캡션 생성을 검증하는 테스트를 추가했습니다.
- `v1.4.0` 자산과 신규 보관함 확대 보기 파일이 Service Worker 앱 셸에 포함되는지 확인하는 회귀 테스트를 추가했습니다.

## [1.3.1] - 2026-08-07

### 수정

- 프롬프트 등록·수정 팝업을 열 때 제목 입력란에 자동으로 포커스하던 동작을 제거했습니다.
- 모바일에서 팝업을 여는 즉시 소프트 키보드가 올라오지 않도록 했습니다.
- 변경된 스크립트가 설치형 PWA에도 즉시 반영되도록 정적 자산 버전과 Service Worker 앱 셸 캐시를 갱신했습니다.

### 테스트

- 편집기 열기 로직에 제목 자동 포커스 호출이 다시 추가되지 않는지 확인하는 회귀 테스트를 추가했습니다.

## [1.3.0] - 2026-08-07

### 추가

- 프롬프트 카드 우측 상단에 항상 보이는 `☆`/`★` 아이콘을 추가해 목록 화면에서 즐겨찾기를 바로 등록하거나 해제할 수 있도록 했습니다.
- 카드 즐겨찾기 아이콘은 터치·클릭뿐 아니라 키보드 Enter와 Space 입력도 지원하도록 했습니다.

### 변경

- 프롬프트 등록·수정 화면에서 즐겨찾기 체크 UI를 제거했습니다. 기존 프롬프트의 즐겨찾기 값은 편집 후에도 그대로 유지됩니다.
- 편집 화면의 LLM, 제목, 본문, 태그 및 이미지 첨부 영역 사이의 세로 간격을 조금씩 줄여 모바일 화면에서 이미지 추가 버튼이 더 빨리 보이도록 했습니다.
- 신규 즐겨찾기 제어 모듈과 편집 화면 스타일이 오프라인 PWA에도 포함되도록 Service Worker 앱 셸 캐시를 갱신했습니다.

### 테스트

- 즐겨찾기 토글이 기존 프롬프트 데이터를 보존하면서 `isFavorite`과 수정 일시만 변경하는지 확인하는 테스트를 추가했습니다.
- 등록·수정 화면에 즐겨찾기 UI가 노출되지 않는지, 카드 별 아이콘 모듈과 신규 스타일이 캐시에 포함되는지 확인하는 회귀 테스트를 추가했습니다.

## [1.2.1] - 2026-08-07

### 변경

- 프롬프트 등록·수정 화면의 본문 입력란 기본 표시 높이를 10행에서 7행으로 줄였습니다.
- 변경된 편집 화면이 설치형 PWA에도 반영되도록 정적 자산 버전과 Service Worker 앱 셸 캐시를 갱신했습니다.

### 테스트

- 본문 입력란이 `rows="7"`을 사용하는지 확인하는 회귀 테스트를 추가했습니다.

## [1.2.0] - 2026-08-07

### 추가

- 본문 붙여넣기 버튼으로 프롬프트를 입력할 때 제목 필드가 비어 있으면 본문의 `제목` 또는 `타이틀` 라벨에서 제목을 자동 추출하도록 했습니다.
- `[제목]`, `제목:`, `【타이틀】`, `*** 제목 ***`처럼 라벨 앞뒤에 공백, 구두점, 특수문자 또는 기호가 있는 형식을 정규식으로 인식하도록 했습니다.
- 라벨과 같은 줄에 값이 있으면 해당 값을 사용하고, 같은 줄이 비어 있으면 다음 유효 한 줄을 제목으로 사용하도록 했습니다.

### 변경

- 자동 추출한 제목은 현재 제목 최대 길이를 넘지 않도록 문자 단위로 제한하고, 사용자가 이미 입력한 제목은 덮어쓰지 않도록 했습니다.
- 신규 제목 추출 모듈이 설치형 PWA의 오프라인 실행에도 포함되도록 Service Worker 앱 셸 캐시를 갱신했습니다.

### 테스트

- `제목`·`타이틀`, 같은 줄·다음 줄, 앞뒤 특수문자, CRLF, 최대 길이, 오탐 방지 및 다음 섹션 헤더 차단 케이스를 추가했습니다.

## [1.1.3] - 2026-08-06

### 수정

- 설치형 PWA가 이전 버전의 HTML과 정적 파일을 계속 표시하지 않도록 서비스 워커 등록 시 HTTP 캐시를 사용하지 않고 즉시 업데이트를 확인하도록 보강했습니다.
- 새 서비스 워커가 적용되면 캐시 무효화 쿼리를 붙여 화면을 다시 불러오도록 했으며, 편집 중인 경우에는 편집 화면을 닫은 뒤 갱신하도록 했습니다.
- 네트워크에서 앱 셸을 가져올 때 서비스 워커 캐시 버전을 쿼리에 포함해 GitHub Pages의 이전 응답 캐시를 우회하도록 했습니다.
- 프롬프트 목록의 태그 영역 위에 여백을 추가해 LLM 필터 버튼과 맞닿거나 겹쳐 보이던 배치를 수정했습니다.
- 동적으로 불러오는 목록 제어 및 보관함 6열 스타일의 캐시 버전을 `v1.1.3`으로 맞췄습니다.

### 테스트

- 서비스 워커 및 업데이트 관리자 JavaScript 구문 검사를 추가했습니다.
- 버전 문자열, 서비스 워커 캐시 이름, 업데이트 확인 옵션 및 태그 영역 여백을 확인하는 회귀 테스트를 추가했습니다.

## [1.1.2] - 2026-08-06

### 수정

- 상세 화면에서 태그 영역이 본문과 같은 가변 행을 사용해 겹치던 문제를 수정하고 날짜, 태그, 본문, 첨부 이미지를 독립된 그리드 행으로 배치했습니다.
- 등록·수정 화면의 태그 칩 높이와 내부 여백을 줄이고 삭제 버튼의 전역 최소 높이를 재정의해 태그와 `×` 사이 간격을 축소했습니다.
- 변경된 태그 UI가 설치된 PWA의 오프라인 실행에도 반영되도록 Service Worker 캐시를 갱신했습니다.

### 테스트

- 상세 태그와 본문이 서로 다른 그리드 행을 사용하는지 확인하는 회귀 테스트를 추가했습니다.
- 태그 삭제 버튼과 태그 칩의 최소 높이 및 간격을 확인하는 회귀 테스트를 추가했습니다.

## [1.1.1] - 2026-08-06

### 변경

- 프롬프트 분류에서 컬렉션 입력, 목록 표시, 상세 표시, 검색 및 필터를 제거하고 태그 기능만 유지했습니다.
- 기존 프롬프트에 저장된 컬렉션 값은 자동 삭제하지 않고 ZIP·JSON 백업 및 복원 호환성을 유지하도록 했습니다.
- 이미지 상세보기는 화면을 채우기 위해 자르는 방식 대신 원본 전체가 보이는 `contain` 방식과 동적 뷰포트 크기를 기준으로 표시하도록 보강했습니다.

### 수정

- 가로·세로 화면 전환이나 모바일 브라우저 표시 영역 변경 후 기존 확대·이동 상태가 남아 이미지가 잘려 보이던 문제를 수정했습니다.
- 뷰어 화면 전환 시 스와이프 미리보기 이미지와 변환 상태를 정리하도록 했습니다.
- 신규 정적 파일과 `v1.1.1` 자산을 오프라인에서 사용할 수 있도록 Service Worker 캐시를 갱신했습니다.

### 테스트

- 이미지 뷰어 transform의 배율 및 이동 상태 판정 테스트를 추가했습니다.
- 프롬프트 검색에서 컬렉션을 제외하고 제목, 본문, 태그만 검색하는지 확인하는 테스트를 추가했습니다.
- 태그 필터 옵션의 사용 횟수 및 이름 정렬 테스트를 추가했습니다.

## [1.1.0] - 2026-08-06

### 추가

- 프롬프트마다 하나의 컬렉션과 최대 20개의 사용자 태그를 지정할 수 있도록 했습니다.
- 컬렉션 및 사용 빈도순 태그 필터를 프롬프트 목록에 추가했습니다.
- 제목과 본문뿐 아니라 컬렉션과 태그도 검색 대상에 포함했습니다.
- 프롬프트와 첨부 이미지를 하나의 ZIP 파일로 내보내는 통합 백업을 추가했습니다.
- ZIP 백업에 매니페스트, 파일 목록, 크기 및 CRC32 체크섬 검증을 추가했습니다.
- 복원 전에 프롬프트 수, 이미지 수, 중복 제외 결과를 확인할 수 있도록 했습니다.

### 변경

- 기본 백업 형식을 이미지 데이터가 포함된 단일 JSON에서 `manifest.json`, `prompts.json`, 이미지 파일로 구성된 ZIP으로 변경했습니다.
- 기존 schemaVersion 1·2 JSON 백업은 계속 복원할 수 있도록 하위 호환성을 유지했습니다.
- 컬렉션과 태그 필터 상태를 재접속 후에도 유지하도록 변경했습니다.
- 기능 추가 규모에 따라 앱 버전을 `v1.0.11`에서 `v1.1.0`으로 올렸습니다.

### 테스트

- 컬렉션·태그 정규화, 검색 및 중복 제거 테스트를 추가했습니다.
- ZIP 파일 생성·해석, 유니코드 파일명, 이미지 분리 저장 및 복원 테스트를 추가했습니다.
- 손상 파일 체크섬 거부와 기존 JSON 백업 호환성 테스트를 추가했습니다.

### 수정

- 컬렉션이나 태그만 수정한 경우에도 저장되지 않은 변경 사항 경고가 동작하도록 편집기 이탈 검사를 확장했습니다.
- 신규 기능이 설치된 PWA의 오프라인 실행에도 반영되도록 Service Worker 캐시를 갱신했습니다.

## [1.0.11] - 2026-08-06

### 추가

- 앱 정보에서 버전별 변경 사항을 확인할 수 있는 릴리즈 노트 화면을 추가했습니다.
- 릴리즈 노트에서 최신 버전을 펼쳐 보여주고 이전 버전은 버전별로 열어볼 수 있도록 했습니다.

### 변경

- 로컬 저장소의 할당량이 1,000MB 이상이면 GB 단위로 표시하도록 변경했습니다.

### 테스트

- MB·GB 경계값과 천 단위 구분 숫자의 할당량 변환 테스트를 추가했습니다.
- 변경 이력 Markdown의 버전, 날짜, 분류 및 항목 파싱 테스트를 추가했습니다.

### 수정

- 저장소 단위 변환과 릴리즈 노트 화면이 오프라인 실행에서도 적용되도록 Service Worker 캐시를 갱신했습니다.

## [1.0.10] - 2026-08-06

### 변경

- 사용자가 마지막으로 선택한 프롬프트, 보관함 또는 설정 탭을 로컬에 저장하도록 변경했습니다.
- 새로고침하거나 설치된 PWA를 다시 실행하면 마지막으로 보고 있던 탭을 자동으로 복원하도록 했습니다.

### 테스트

- 지원 탭 판정과 잘못된 저장값의 기본 탭 대체 테스트를 추가했습니다.

### 수정

- 마지막 탭 복원 모듈이 오프라인 실행에서도 적용되도록 Service Worker 캐시를 갱신했습니다.

## [1.0.9] - 2026-08-06

### 변경

- 이미지 스와이프 중 현재 이미지가 손가락 이동을 따라가고 다음 이미지가 화면 가장자리에서 함께 진입하도록 변경했습니다.
- 스와이프 완료, 짧은 제스처 복귀 및 키보드 이동에 감속 곡선을 적용해 전환을 부드럽게 했습니다.
- 첫 번째·마지막 이미지에서 이동 범위를 넘기면 탄성 저항을 적용하고 자연스럽게 원위치로 복귀하도록 했습니다.

### 테스트

- 이동 가능 방향의 드래그 오프셋과 목록 경계의 저항 계산 테스트를 추가했습니다.

### 수정

- 부드러운 이미지 전환 모듈이 설치된 PWA에도 반영되도록 Service Worker 캐시를 갱신했습니다.

## [1.0.8] - 2026-08-06

### 추가

- 상세 이미지 뷰어와 이미지 보관함에서 좌우 스와이프로 이전·다음 이미지를 탐색할 수 있도록 했습니다.
- 키보드 환경에서도 좌우 방향키로 이미지를 이동할 수 있도록 했습니다.

### 변경

- 보관함의 프롬프트별 보기에서는 선택한 프롬프트 그룹의 이미지 안에서만 이동하도록 범위를 제한했습니다.
- 프롬프트 복제 시 제목과 본문만 복제하고 첨부 이미지는 제외하도록 변경했습니다.

### 테스트

- 스와이프 방향 판정, 이미지 인덱스 경계 처리 및 범위 기준 캡션 생성 테스트를 추가했습니다.

### 수정

- 신규 이미지 탐색 모듈이 설치된 PWA에서도 즉시 반영되도록 Service Worker 캐시를 갱신했습니다.

## [1.0.7] - 2026-08-06

### 수정

- 보관함 6열 보기에서 프롬프트별 그룹 컨테이너가 전체 너비를 유지하도록 수정했습니다.
- 보관함 6열 보기 모듈과 Service Worker 캐시 버전을 동기화해 설치된 PWA에 변경 사항이 반영되도록 했습니다.

## [1.0.6] - 2026-08-06

### 추가

- 보관함에 6열 썸네일 보기 옵션을 추가했습니다.
- 선택한 보관함 열 수가 재접속 후에도 유지되도록 저장 기능을 추가했습니다.

### 수정

- 보관함 UI가 생성되기 전에 6열 제어 기능이 초기화되던 문제를 수정했습니다.

## [1.0.5] - 2026-08-06

### 변경

- 프롬프트 목록의 검색 조건, LLM 필터, 정렬 기준 및 즐겨찾기만 보기 상태를 로컬에 저장하도록 변경했습니다.
- 정렬과 즐겨찾기 제어 영역을 재배치하고 목록 제어 UI를 정리했습니다.

### 수정

- 목록 제어 모듈 초기화 이후 저장된 조건이 안정적으로 적용되도록 수정했습니다.

## [1.0.4] - 2026-08-06

### 추가

- 이미지 보관함 상단에 LLM별 필터를 추가했습니다.

### 수정

- 필터에서 제외된 보관함 항목이 확실하게 숨겨지도록 수정했습니다.
- 오래된 스크립트가 화면의 앱 버전을 이전 값으로 덮어쓰지 못하도록 버전 표시 기준을 단일화했습니다.

## [1.0.3] - 2026-08-06

### 수정

- 작은 화면에서 보관함 카드와 컨트롤이 잘리거나 가로 스크롤을 만드는 문제를 수정했습니다.
- 모바일 보관함의 간격과 카드 배치를 조정했습니다.
- 변경된 보관함 UI가 즉시 반영되도록 오프라인 캐시를 갱신했습니다.

## [1.0.2] - 2026-08-06

### 추가

- 동일한 프롬프트에 첨부된 이미지끼리 묶어 보는 그룹형 보관함 보기를 추가했습니다.
- 그룹형 보관함 전용 레이아웃과 스타일을 추가했습니다.

### 변경

- 이미지 보관함과 전체 화면 이미지 뷰어의 뒤로가기 및 브라우저 기록 처리를 보강했습니다.

### 수정

- `v1.0.2` 정적 자산의 캐시 무효화 쿼리와 화면 표시 버전을 일치시켰습니다.

## [1.0.1] - 2026-08-06

### 추가

- 모든 첨부 이미지를 한곳에서 확인하는 이미지 보관함 탭을 추가했습니다.
- 새 프롬프트 동작을 우측 하단 원형 플로팅 버튼으로 제공하도록 변경했습니다.

### 변경

- 기존 보관함 탭을 프롬프트 탭으로 정리하고 목록 및 상세 화면의 메타 정보 배치를 개선했습니다.
- 이미지 보관함과 프롬프트 목록의 카드 및 제목 스타일을 정리했습니다.

## [1.0.0] - 2026-08-06

### 변경

- 초기 안정 버전으로 앱 버전을 `1.0.0`으로 전환했습니다.
- 상세 팝업의 상단 정보와 하단 동작 영역을 고정하고, 가운데 본문만 세로 스크롤되도록 레이아웃을 변경했습니다.
- 첨부 이미지가 있는 경우 상세 화면 하단에서 안정적으로 표시되도록 배치했습니다.

### 수정

- 상세 팝업의 가로 넘침과 스크롤 충돌을 수정했습니다.
- 변경된 상세 레이아웃이 설치된 PWA에도 반영되도록 캐시를 갱신했습니다.

## [0.2.8] - 2026-08-05

### 수정

- Android 환경에서 사진 선택 후 이미지가 첨부되지 않던 파일 선택 문제를 수정했습니다.
- 수정된 이미지 첨부 코드가 오프라인 PWA에 반영되도록 캐시를 갱신했습니다.

## [0.2.7] - 2026-08-05

### 추가

- 프롬프트별 이미지 첨부 기능을 추가했습니다.
- 첨부 이미지를 IndexedDB에 저장하고 편집 및 상세 화면에서 미리 볼 수 있도록 했습니다.
- 이미지 썸네일 가로 스크롤과 전체 화면 이미지 보기를 추가했습니다.
- 이미지 확대 및 이동이 가능한 뷰어 UI를 추가했습니다.

### 변경

- 편집 화면의 입력 필드와 이미지 갤러리 배치를 정리했습니다.
- README에 이미지 저장 및 백업 동작을 문서화했습니다.

## [0.2.6] - 2026-08-05

### 추가

- 본문 입력란에 전체 지우기와 클립보드 붙여넣기 동작을 추가했습니다.
- 메인 화면 제목 옆과 앱 정보 화면에 앱 버전을 표시하도록 했습니다.
- 동일 제목 프롬프트에 순차 버전을 부여하고 목록 및 상세 화면에 표시하도록 했습니다.
- 신규 프롬프트 작성 시 마지막으로 사용한 LLM 선택을 기억하도록 했습니다.

### 변경

- 제목 입력 최대 길이를 50자로 조정했습니다.
- 카드 본문, 버전 배지 및 편집기 높이를 모바일 화면에 맞게 조정했습니다.

## [0.2.4] - 2026-08-05

### 추가

- 저장된 프롬프트를 새 작성 화면으로 복제하는 기능을 추가했습니다.
- 복제 직후에는 저장하지 않고 사용자가 저장 버튼을 눌렀을 때만 새 항목이 생성되도록 했습니다.

### 변경

- 목록의 정렬 및 즐겨찾기 제어 영역을 더 작은 너비로 정리했습니다.
- 당시 제목 입력 제한을 30자로 적용했습니다. 이후 `v0.2.6`에서 50자로 확대했습니다.

## [0.2.0] - 2026-08-05

### 추가

- 프롬프트 등록, 조회, 수정, 삭제 기능을 제공하는 초기 PWA를 구현했습니다.
- ChatGPT, Gemini, Grok 및 Claude 분류를 추가했습니다.
- 제목·본문 검색, LLM 필터, 정렬, 즐겨찾기 및 본문 복사를 추가했습니다.
- 브라우저 IndexedDB 기반 로컬 저장과 JSON 백업·복원을 추가했습니다.
- 반응형 UI, 라이트·다크 테마, 앱 아이콘 및 설치 가능한 웹 앱 매니페스트를 추가했습니다.
- Service Worker 기반 오프라인 실행과 GitHub Pages 배포 구성을 추가했습니다.

### 변경

- 외부 API를 사용하는 프롬프트 변환 UI를 제거하고 완전한 로컬 프롬프트 관리 기능에 집중하도록 범위를 정리했습니다.

## 과거 버전 기록 기준

- 저장소에서 명시적으로 확인되는 최초 버전 표시는 `v0.2.0`입니다.
- `v0.2.1`~`v0.2.3`, `v0.2.5`는 별도 버전 변경 또는 릴리즈 근거가 확인되지 않아 항목을 임의로 만들지 않았습니다.
- 이후 릴리즈부터는 버전 증가와 동시에 이 문서의 해당 버전 항목을 반드시 추가합니다.
